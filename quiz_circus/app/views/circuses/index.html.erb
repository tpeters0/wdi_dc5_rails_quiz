<h1> Circuses </h1>
      <li><a href="/circuses/new"></a>Add Circus</li>
<% @circuses.each do |circus| %>
  <p><%= link_to("#{circus.name}", circus) %></p>

  <p><%=image_tag(circus.image_url, width: 400, alt: "#{circus.name} picture") %></p>

<% end %>
